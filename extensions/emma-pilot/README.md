# Emma Pilot Extension

Static Manifest V3 extension package for Chrome and Microsoft Edge.

Build local release artifacts:

```bash
pnpm build:emma-pilot
```

Artifacts are written to `public/emma-pilot/releases/`.
